const para2 = document.getElementById('master');
const input2 = document.getElementById('number');
const btn2 = document.getElementById('generate');



/*btn2.addEventListener('click', function () {
    para2.textContent = 'Output: ';
    let num = input2.value;
    input.value = '';
    input.focus();
    for (let i = 1; i <= num; i++) {
        let sqRoot = Math.sqrt(i);
        if (Math.floor(sqRoot) !== sqRoot) {
            continue;
        }

        para2.textContent += i + " ";
    }

});*/